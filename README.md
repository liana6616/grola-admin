# !!! Этот файл не загружать в боевые версии !!! #
--------------------------------------------------


# Начало работы:

1. cd gulp
2. npm install
3. cd .. (просто чтобы вернуться в корень)
4. composer update

# Далее для запуска сборки

1. cd gulp
2. gulp

Собирается из папки
/public/resource
в
/public/src

# База данных

Начальная в папке /config/db.sql

# Вход в админку

/visualteam
test@test.ru
test


# Настройки в папке /config

# /config/admin.php - управление меню админки
# /config/modules/ - настройки модулей админки

# /config/db.php - данные для подключения к БД
# /config/routes.php - маршруты


# В модулях где используются черновики вывод на сайт нужно делать с условием `is_draft`=0
# если админ авторизован давать доступ к `is_draft`=1 для предпросмотра черновиков перед публикацией